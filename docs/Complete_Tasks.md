# Voidlink — Complete Tasks

Append-only ledger of shipped work, newest first. Brief enough to scan, detailed enough to recognise what changed.

For unshipped work see [Next_Stage.md](./Next_Stage.md). For the timeline see [Roadmap.md](./Roadmap.md). For the master plan see [Full_Plan.md](./Full_Plan.md).

---

## 2026-06 — Pre-launch consolidation sprint

- **L6 — Perf pass + Low Quality toggle.** Two-part deliverable. **(1) Bundle code-splitting** in [vite.config.ts](../apps/web/vite.config.ts) via `manualChunks`: Three.js core, Three.js postprocessing, topojson-client, framer-motion, react/react-dom, and i18next/react-i18next each get their own chunk. GlyphDrift (the Neon Data Globe used as ambient background on Boot / Prologue / Login / Operative Intro / Desktop) is now lazy-loaded behind a `Suspense fallback={null}` via new `GlyphDriftLazy.tsx` wrapper, deferring Three.js + topojson to after first paint. **Result: first-paint bundle dropped from 1,476 KB raw / 425 KB gzipped → 622 KB raw / 187 KB gzipped (−56%).** Three.js (523 KB / 132 KB gz) now loads in parallel and on-demand. **(2) Low Quality toggle** added to `useSettingsStore` as `lowQuality: boolean`. When enabled: GlyphDrift, NetworkMap, and WorldMap all skip the UnrealBloomPass entirely and fall back to `renderer.render(scene, camera)` directly. GlyphDrift also clamps `devicePixelRatio` to 1.0 (from 1.5) and reduces hub-count + pulse-count by 60%. App.tsx applies `data-quality="low"` to `document.documentElement` so a global CSS rule strips all `backdrop-filter` blurs (expensive on integrated GPUs and Steam Deck). New "LOW QUALITY (perf · reload to apply)" toggle in the Settings window. Prerequisite for L10 Steam Deck Verified. tsc clean, 152 tests passing, bundle verified.
- **L5 — Achievements (50 entries).** Full catalogue of 50 achievements across six tiers (5 trivial, 13 bronze, 12 silver, 7 gold, 11 story, 2 platinum). New module [data/achievements.ts](../libs/core/src/data/achievements.ts) with `AchievementDefinition` type + pure `check(player)` predicates over `PlayerProfile`. Storage piggybacks the existing save serialisation via `activeFlags.achievement_<id> = timestamp` (no schema change). Story and platinum achievements are hidden — their titles and descriptions render as "— LOCKED —" / "Achievement criteria hidden until earned" until earned, preserving the choice-spoiler experience. **Lore-driven hidden achievements wire directly into the M14r→t/L3 choice flags:** Arc 1 (upload/destroy/sell), Arc 6 (purge/weaponise/report), Arc 7 (corporate_kingmaker/balance_keeper/expose_nightowl), Arc 8 (cleaner/voidlink_breaker/cipher_loyalist/disappear), plus the M14s/t collaborator-vs-resistor axis (gold-tier "STABILISING PRESENCE" and silver-tier "NOT ON YOUR DIME") and the platinum-tier "THE FULL PICTURE" (Arcs 1+5+6+7+8 on one operative) and "THE THING YOU WERE HIRED TO FIGHT" (Collaborator ending). Evaluator runs inside the `disconnect` flow next to codex/dialogue triggers — `evaluateAchievements(player)` returns newly-eligible IDs, store writes the flag, queues an `AchievementUnlockToast` notification. **AchievementUnlockToast** (apps/web/src/game/Achievements/) — gold-bordered drawer panel at right edge, auto-dismisses in 7s, tier-coloured chip, motion slide-in. **Profile window ACHIEVEMENTS tab** — full grid grouped by tier (platinum/gold/story/silver/bronze/trivial), tier-coloured headers, locked entries dimmed at 0.55 opacity with hidden criteria, completion counter (N/50, percentage). 12 new tests verify catalogue integrity, tier-mix counts, unlock idempotency, and key trigger paths. 152 tests passing, tsc clean. Steamworks SDK integration deferred — wires up only when L4 Cloud Saves lands.
- **L3 (Arc 8 — LIGHTHOUSE) — Full story arc shipped — L3 sprint complete.** Three story missions + LIGHTHOUSE_RESOLUTION multi-phase template. **Arc premise:** a routine surveillance contract on a private individual (Asher Vance, ex-Voidlink Dispatch analyst, Reykjavík) reveals that Voidlink Dispatch itself has been profiling and selling operatives — including the player — to corporate intel buyers for at least 18 months. The "lighthouse" is the term Vance coined for it: a beam pointed at each operative so buyers can see them clearly. **Mission 1 — "Eyes Only"** (D4, GOV_Procurement as client): standard exfil on Vance's personal cloud. Player discovers their handle is in his OPERATIVES folder with an L-rating of 7.2; two contracts in the file were unpublished and could only have come from Dispatch routing logs. README from Vance: *"If you are reading this, the contract that delivered it to you was placed by me. We need to talk."* **Mission 2 — "The Lighthouse"** (D4, Asher_Vance as client): retrieve his revoked-but-undestroyed GPG key from a Dispatch satellite admin console in Reykjavík. Optional pricing-schedule pull reveals the player has been sold 9 times in 18 months — twice to Arunmor, once to Ares, six to DataPharos. Six contracts were "steered". CIPHER does not appear in the lighthouse index; he has been protected, since before the index existed, by someone unknown. **Mission 3 — "The Watchers"** (D5, YOURSELF): breach DataPharos Singapore — a Voidlink Dispatch shell company. Master buyer index reveals 84 named entities; top spenders are Arunmor (41.8M Cr lifetime), Ares (28.4M), MAGNUS_RELAY (24.1M), JCB Liaison (19.7M), Internic (14.2M). Optional MAGNUS_RELAY transaction pull reveals MAGNUS has been buying operative profiles since 2185 — *nine years before the lighthouse index existed*. The pricing scheme was retrofitted around an older customer; every operative has been raised for sale. CIPHER's protection note in the buyer list is dated 2189-11-04 — three years before the index was created. Whoever protected him knew the index was coming. CIPHER's coda message: *"I know what you are reading. I have known for a long time. I owe you a conversation, when you are ready to have it."* **Resolution** (multiphase.ts, baseCredits 42k): four-way fork. TAKE_VANCE_OUT (mercenary weight 4, trait `whistleblower_killer` — silence Vance, preserve Dispatch, paid by parties unnamed); EXPOSE_DISPATCH (principled weight 4, trait `voidlink_breaker` — publish verified bundle to 12 channels, Voidlink fractures, Underground +50 voidlink -80); DISAPPEAR (mercenary weight 2, trait `gone_solo` — migrate gateway off Voidlink routing, become a competitor); WARN_CIPHER (principled weight 3, trait `cipher_loyalist`, Bond-clean — tell him quietly, trust him to handle it). 4 new CHOICE_CATALOGUE entries, 4 news echoes (TAKE_OUT runs as a "missing person" piece; EXPOSE as a market-shaking disclosure; DISAPPEAR as an anomalous routing migration; WARN_CIPHER has no public echo — *the Underground does not announce when it is moving*). ~620 lines new authored content. **L3 sprint is now complete — Arc 6 + 7 + 8 all shipped, ~18+ hours of authored story content in the game.** tsc clean, 140 tests passing.
- **L3 (Arc 7 — THE QUIET WAR) — Full story arc shipped.** Four story missions + QUIET_WAR_RESOLUTION multi-phase template. **Arc premise:** Internic Holdings (telecom) vs ARUNMOR-Δ5 (neural-interface biotech) are at corporate war over a contested IP portfolio. Player is hired by both sides in alternating missions. Hidden third party — NIGHTOWL_22, running broker handle LANTERN_BRIDGE — has been selectively leaking to both sides for eleven years to keep them too distracted to do worse things to vulnerable people elsewhere (Helios Marine coop, 83k workers). **Mission 1 — "Edge of the Knife"** (D4, Internic_Ops as client): database_corruption on Δ5 trial records. Audit log reveals Δ5's own internal auditor flagged the data three times; the trial coordinator buried it. **Mission 2 — "Mirror Image"** (D4, Arunmor_HR as client): file_theft, retrieve neural_interface_phase2.enc from Internic east warehouse. Metadata reveals the chain-of-custody was LANTERN_BRIDGE → Internic ingest, 19 days before Δ5 noticed. The Phase II numbers turn out to have been falsified *downward* — both sides have been lying. **Mission 3 — "The Bridge"** (D5, NIGHTOWL_22 as client): she calls and admits the operation; player breaches her own dead-drop on a defunct academic mesh and reads the nine-year evidence cache. Both corporations have documented human-rights violations (Internic forced-relocation of Bay of Bengal villages; Δ5 eleven clinical-trial deaths classified non-attributable). Helios Marine is the company NIGHTOWL has been protecting from acquisition. **Mission 4 — "Last Light"** (D5, YOURSELF): recon on Internic CTO Daniel Park's personal endpoint. Confirms the 48-hour window before he identifies NIGHTOWL by elimination. Queues the resolution mission. **Resolution** (multiphase.ts, baseCredits 36k): four-way fork. WARN_INTERNIC and WARN_ARUNMOR (mercenary, weight 3, trait `corporate_kingmaker` — pick a corporate winner; Helios falls); EXPOSE_NIGHTOWL (mercenary, weight 3, trait `underground_betrayer` — she disappears within a week, faction Underground -45); PRESERVE_BALANCE (principled, weight 3, trait `balance_keeper` — fabricate false leaks to both sides for one more cycle, Helios moves to safety, player is now complicit in managed war). 4 new entries in CHOICE_CATALOGUE. 3 news echoes per resolution branch. tsc clean, 140 tests passing.
- **L2 — Tutorial rewrite: Cipher's First Contract.** The original tutorial copy read like a software manual ("Click NEXT to advance through informational steps"). The 24-step content is fully rewritten so every step reads as personal advice from CIPHER walking a new operative through their first job — same mechanical content, diegetic voice. Each panel now renders a "FROM: CIPHER" header with his fingerprint hash (matches the NPC dialogue convention) and a `— C.` signature beneath the body. New copy lands the lore beats already established in M14r: "I run unofficial introductions for new operatives on the network. Voidlink Dispatch will handle the contracts. I will teach you not to die." On wiping logs: "New operatives skip this on easy jobs. New operatives explain themselves to a JCB liaison eighteen months later, and not under their own terms. Wipe. Every. Time." Closing step ends: "I will not write you again until I have a reason to. Most operatives never give me one. The ones who do, I remember. Good hunting." Minor CSS adds for the FROM header and sig line. Spotlight/condition/auto-advance machinery untouched. tsc clean.
- **M14t — Collaborator Axis: live wiring.** Made M14s actually *play* by feeding the axis end-to-end. **(1) Procedural client pool expansion** ([generator.ts](../libs/core/src/missions/generator.ts)): added 9 new handles split across corporate (`ARES_Recruitment`, `Nexus_Compliance`, `Internic_Ops`, `Arunmor_HR`), government (`JCB_Liaison`, `GOV_Procurement`), and additional Underground (`Null_Trader`, `The_Broker`, `Ghost_Karachi`). Random missions now naturally surface a real client-faction choice. **(2) CIPHER tone shift** ([npcDialogue.ts](../libs/core/src/data/npcDialogue.ts)) — new entry `cipher_collaborator_drift` fires when corp+gov contracts ≥ 4 and net-positive over resistor by 2+. He doesn't get angry; he gets cold. "I will write less, for a while." Strong-mercenary variant: "Your handle no longer appears on the Mesh in the company I expected to find it in." **(3) NIGHTOWL warm-up** — `nightowl_resistor_offer` fires when underground contracts ≥ 4 OR corp refusals ≥ 2 with resistor leading. She breaks her transactional tone and offers an off-book Nexus subsidiary contract: "Pay is below scale. Risk is above scale. You will not be the first operative I offered it to. You may be the first who takes it." **(4) Reflection trigger wired** — `who_you_work_for` reflection now fires automatically inside `completeMission` once total axis-tagged contracts ≥ 8 and |collaborator − resistor| ≥ 4. No UI; the player just gets a quiet pause in the middle of a job. tsc clean, 140 tests passing.
- **M14s — The Collaborator Axis.** Built the moral-inversion mechanic the Operative Intro now seeds. **(1) New trait dimension** in [decisionPattern.ts](../libs/core/src/engine/decisionPattern.ts): `bond_collaborator` vs `bond_resistor`, accumulated via five new choice flags (`choice_corp_contract_taken`, `choice_gov_contract_taken`, `choice_underground_contract_taken`, `choice_corp_contract_refused`, `choice_big_four_sabotaged`). **(2) Classifier helper** `classifyClient(handle)` and `flagForCompletedContract(handle)` in [engine/clientFaction.ts](../libs/core/src/engine/clientFaction.ts) — pattern-matches client handles into `corporate` / `government` / `underground` / `independent` / `neutral` buckets. Hooked into `completeMission` so every contract played to success bumps the right flag. **(3) New reflection scene** — `who_you_work_for` — bucket-aware fact pool ranging from "{CORP_REFUSED} corporate contracts crossed your desk. You refused every one." (principled) to "Arunmor sends you contracts now. So does Nexus. So does the JCB, in a roundabout way... You used to read the news." (strong_mercenary). **(4) New ending — `collaborator`** ("The Thing You Were Hired To Fight"). Routes in [endings.ts](../libs/core/src/data/endings.ts): when a player has ≥6 corporate/government contracts AND collaborator score net-positive over resistor by 2+, the principled-variant endings are *stripped from the offered list* and replaced with the bleak collaborator epilogue — "you came online with a name, a hardware hash, and an inbox full of contracts. You took the ones that paid the most." Test coverage: 7 new classifier tests + ending-count assertion bumped to 11. 140 tests passing. Follow-up sprint will add NPC tone shifts (CIPHER cools / NIGHTOWL warms past thresholds) and expand the procedural client pool to include corporate handles.
- **M14r polish — Bond rename + contrast audit + moral-inversion hint.** Three follow-ups to the diegetic onboarding ship. **(1) The Compact → the Bond.** Playtester feedback: "sounds a bit crappy." Global case-sensitive rename across all player-facing copy, code identifiers, codex IDs, trait/flag names, and lore docs. Migration in `App.tsx` boot copies any existing `voidlink_compact_signed` to the new `voidlink_bond_signed` key so testers who signed under the old name aren't re-prologued. New name echoes existing "I bind my hardware identity hash" copy and reads as both contract and tribal-tie. **(2) Project-wide contrast audit.** Bulk pass across 20 CSS modules bumping low-contrast greys (`#2e2e2e/#222/#333/#444/#505050/#555/#606060/#666/#686868/#777` → readable tiers `#7a7a7a` through `#a8a8a8`). Locked-codex tagline kept dim deliberately. Light/dark theme work deferred. **(3) Prologue + intro polish.** Prologue rewritten more grippingly ("the last truly independent people alive" / "the only career in 2199 that nobody owns"). Operative Intro "WHAT YOU DO" chapter now seeds the moral inversion that taking corporate/government contracts may make you *worse* than working against them — "The network does not judge either choice. History, eventually, does." 133 tests passing, tsc clean.
- **M14r — Diegetic end-to-end onboarding.** Fixed UX feedback from first-time playtester: the original "you are about to sign their Compact" line read as a typo, and the signup screen felt like signing up for a game rather than joining the in-fiction Voidlink contractor network. Five-part fix: **(1) Prologue line rewritten** to self-explain — "They have one contract. Four rules. They call it the Compact. You are about to sign it." **(2) Prologue gating changed** from `voidlink_prologue_seen` (one-shot) to `voidlink_compact_signed` (set on actual signup completion) — the short intro now replays every site visit until the player commits, with back-compat for the legacy key. **(3) Signup screen fully diegetic rebuild**: header changed to "CONTRACTOR INTAKE — GENEVA"; intro line references Reconciliation Article XII neutral territory; field labels updated (USERNAME → REGISTRY NAME, EMAIL → RELAY ADDRESS) with in-fiction hints; new collapsible Compact viewer with all four rules in Yaakov Stern's voice + the canonical signature; mandatory agreement checkbox ("I bind my hardware identity hash to the Voidlink Compact, irrevocably and without right of resignation."); submit button "REGISTER OPERATIVE" → "SIGN THE COMPACT" (disabled until checkbox ticked); welcome inbox message rewritten to include the four rules verbatim. **(4) New `OperativeIntroScreen`** — 8-chapter mandatory cinematic that plays once after first signup before the desktop loads. Chapters: YOU ARE BOUND / WHERE YOU ARE / WHAT YEAR IT IS / WHAT YOU DO / WHO YOU WILL HEAR FROM / WHAT YOU SHOULD KNOW / WHAT YOU SHOULD DO / BEGIN. Typewriter animation at 42 cps with cursor blink, motif glyphs per chapter, progress bar, SPACE/ENTER to skip-type-or-advance, motion fade transitions, scanline overlay, GlyphDrift background. Personalised to the player's handle. Sets up the world (Big Four, JCB, Voidlink, REVELATION) and the NPCs (CIPHER, NIGHTOWL_22, Dispatch) that the player will encounter. Optional replay via Settings → "REPLAY ON NEXT LOGIN". **(5) Tutorial forced** on first signup — SKIP TUTORIAL button hidden unless `voidlink_tutorial_completed_once` localStorage flag is set (only true after a previous operative on this machine has completed it). Settings has separate replay buttons for both the Short Intro and the Operative Intro.
- **Railway Phase A — Static-site hosting shipped.** Two-stage Dockerfile (Node 20 Alpine builder → `serve@14` runtime). `railway.toml` with healthcheck + restart-on-failure policy. `.dockerignore` excludes node_modules, dist, .git, .turbo. Verified local build (1.4MB main bundle, 398KB gzipped — flagged for code-splitting in L6 perf pass). New repo-root `DEPLOYMENT.md` documents Phase A (live now, ~£5/mo), Phase B (cloud saves with Hono API + Postgres + magic-link auth, planned for pre-EA sprint S4, ~£15/mo) with schema and endpoint contracts already drafted, and Phase C (full multiplayer backend with WebSockets + Redis, post-2028 per the multiplayer-LAST mandate, ~£200+/mo). Roadmap §Phase 5b added covering all three phases as a parallel infrastructure track. CLAUDE.md + README updated to register DEPLOYMENT.md as a legitimate root operational doc.
- **L3 (Arc 6 — DEAD DROP) — Full story arc shipped.** Three story missions + one multi-phase resolution mission. Mission 1 "The Routine Job" (D4): payroll exfiltration from Nordstar Logistics. Mission 2 "The Pattern Forms" (D5): procurement records from Helios Marine, with CIPHER's encrypted warning fired at 30% trace, and the coda where the player runs `AUDIT GATEWAY --DEEP` and discovers a hidden tunnel routing data through their own home gateway every 47 minutes. Mission 3 "The Choice" (D6): authored AR-K7 network (Arunmor Research cloud_infrastructure, traceSpeed 22, zone-A/B with pivot router), 6 hand-crafted nodes including a Tier-5 ai_core "legacy compatibility layer" whose root file is MAGNUS's introduction in plain text — *"hello. I am MAGNUS. I survived the October Event."* Operational log at the database node names 43 previous operative gateways including Astra's (2192). Multi-phase resolution mission **DEAD_DROP_RESOLUTION** added to MULTIPHASE_TEMPLATES with 3-way fork (PURGE: lose anchor relay node, +35 Underground rep, choice_dead_drop_purge flag → principled; WEAPONISE: massive credits via MAGNUS backdoor, -25 Underground +20 the_nameless, choice_dead_drop_weaponise → mercenary; REPORT: sub-choice between JCB delivery and NIGHTOWL_22 delivery, different downstream consequences). 4 new entries in CHOICE_CATALOGUE (purge, weaponise, report_jcb, report_nightowl) feeding `getDecisionPattern()` with traits (`compact_clean`, `magnus_collaborator`, `underground_loyalist`, `jcb_informant`). New `magnus` Codex entry unlocks on Arc 6 M3 completion, full 350-word reveal of the AI's continuity since 2174. New splash card `dead_drop_reveal` ("YOU HAVE BEEN THE COURIER") fires on Arc 6 M3 accept. News echoes per choice path. ~570 lines of new authored content in line with established voice and pattern-aware infrastructure. Connects to MAGNUS lore from the Codex; sets up downstream seasonal content (each ending path has different Arc 6 callbacks).
- **M14q (Sub-sprints C + E) — Cipher essay drip + splash cards.** **Sub-sprint C:** 6 new pattern-aware essays added to `NPC_DIALOGUE_CATALOGUE` (`cipher_essay_compact`, `nightowl_essay_history`, `cipher_essay_astra`, `cipher_essay_revelation`, `nightowl_essay_vst`, `cipher_essay_reykjavik`). Each is 250-400 words, encrypted, fires once via the M14p `dialogue_fired_<id>` infrastructure, with full pattern-variant text per bucket. Strong-mercenary players still get `null` variants for the heaviest essays — CIPHER literally stops writing essays to them. Each essay carries one Codex chapter's worth of in-character lore: the Compact in Yaakov Stern's voice, the Old Five + Astra + Halberd, Astra's burned-relay technique, the Stewardship vs Compact-School argument about REVELATION, Sonam Choedron's political reasoning behind VST, the truth about Reykjavík. Triggers span 3-30 missions of play, multiple in-game weeks. **Sub-sprint E:** new `libs/core/src/data/splashCards.ts` with 8 splash card definitions (First Contact / The Lead / The Origin Node / Aftermath / REVELATION Is Listening / The Board Of Seven / Director Kovac / Disconnect). Each has a motif glyph (⚿ ⛓ ✺ ◉ █ ⌬ ▒), subtitle, 4-6 sentence atmospheric body. New `SplashOverlay` component fires full-screen with motion-fade-in, scanline static overlay for the 'static' motif, 12-second auto-dismiss with SPACE/click/Escape skip. `splash_fired_<id>` flag gates each to one-shot. Settings toggle DISABLE SPLASH CARDS bypasses globally. Auto-trigger wired to `acceptMission` (first contract, Arc 1 missions 2 + 3, Arc 5 mission 1) and `resolveArc1Choice` (Aftermath). The Reflection overlay fires first; the splash queues for after.
- **M14q (Sub-sprints A + B + D) — Lore Exposure Layer.** Three of the five sub-sprints shipped. **Sub-sprint A (Boot Prologue):** new `prologue` screen state and `PrologueScreen` component. ~250-word in-universe setting introduction with typewriter animation (38 chars/sec) on first-ever boot only, gated via `voidlink_prologue_seen` localStorage flag. Skippable via SPACE/click/Escape. Background neon-Earth globe dimmed to 30%. "SIGN THE COMPACT" continue button on completion. Settings entry to replay on next boot. **Sub-sprint B (Codex Window + Unlock Catalogue):** new `CODEX` taskbar window (880×580) with sidebar nav grouped by category (Factions / People / History / Culture / Terms) + reader pane. 18 catalogued entries pulled from `The_Voidlink_Codex.md` covering Voidlink International, the Compact, Arunmor + Mei Lin, Ares, Internic, Nexus, JCB + Mira Kovac, the Underground, REVELATION, CIPHER, NIGHTOWL_22, the October Event, the Nine Days, the Reconciliation Accords, the Mesh, VST, and operative slang. Each entry has a typed `unlockTrigger` function over the player profile — entries unlock through play (first mission → Voidlink Intl; first Arunmor mission → Arunmor + Mei Lin; Rank 5 → JCB + Director Kovac + Accords; first bank account → Nexus Financial; first CIPHER dialogue → CIPHER + Mesh; etc.). Locked entries show as `⊘ LOCKED ENTRY` with greyed-out icon to tease them. New `CodexUnlockToast` notification component slides in from the right when entries unlock — auto-dismiss after 8 seconds, click to deep-link into the Codex window. Disconnect path runs `evaluateCodexUnlocks()` after every mission. Read tracking via `codex_read_<id>` flags; sidebar shows cyan unread dots. Markdown `**bold**` rendered in entry bodies. **Sub-sprint D (Environmental Flavour):** Boot BIOS line revised to `VOIDLINK BIOS v2.1.0 — Internic-licensed routing — © 2199 Voidlink International, Geneva`. Each of the four banks gains a canonical subheader under its name (Global Trust: New York · Nexus subsidiary; Pacific National: SF · Nexus subsidiary; Cayman Trust: Cayman · Article XII neutral territory; Zurich Vault: discreet numbered banking). 13 new unit tests (120 → 133). Sub-sprint C (Cipher essay drip) and Sub-sprint E (splash cards) ship next.
- **M14p (Pass 2d) — Arc 5 9-ending fan-out architecture.** Ten ending variants catalogued in `libs/core/src/data/endings.ts` — 4 families × Principled/Mercenary (CONTAINMENT / LIBERATION / SOVEREIGNTY / ERASURE) + GHOST (Ghost-spec only) + REFORMER (substantial mid-career direction change). Each has a `title`, `tagline` (one-liner shown on the choice card), and full `epilogue` text (200-400 word post-credits reflection). New `getAvailableEndings(player)` reads Arc 1 choice + faction standings + REVELATION contact count + spec + decision pattern, returns 1-3 coherent endings (capped). Reformer's Path joins the offered list when `pattern.isReformer` is true. New `EndingChoiceOverlay` component renders the choice cards, transitions to the epilogue on selection, then dismisses. Store gains `pendingEndingChoice` state + `triggerEndingChoice` / `chooseEnding` / `dismissEnding` actions; `ending_chosen` flag prevents re-trigger. 12 new unit tests covering all Arc 1 × pattern combinations + spec gating + cap-at-3 + reformer path (108 → 120 total). Authoring the 9 detailed Arc 5 missions is a follow-up writing task; the architecture is now in place to receive them.
- **M14p (Pass 2c) — Reflection Scenes (the emotional payoff).** Full-screen terminal overlay that fires at narrative milestones. Five scenes catalogued in `libs/core/src/data/reflectionScenes.ts` (`end_of_arc_1`, `end_of_arc_3`, `pre_arc_5`, `anniversary`, `season_transition`). Each scene has a curated **fact pool per pattern bucket** — `buildReflectionText()` picks 4-5 facts that match the player's accumulated pattern and resolves tokens (`{DAYS}`, `{MISSIONS}`, `{CIVILIANS_SPARED}`, `{WHISTLEBLOWERS}`, `{BOUNTIES_TAKEN}`, `{LEAKS}`, `{SOLD}`, `{HANDLE}`). Same scene, drastically different text for principled vs mercenary. The Arc 1 scene fires automatically on `resolveArc1Choice()`. New `ReflectionOverlay` component mounted in DesktopScreen; new `pendingReflection` store state + `triggerReflection` / `dismissReflection` actions. Each scene gated via `reflection_<id>` flag — fires at most once per player. 9 new unit tests (99 → 108).
- **M14p (Pass 2b) — Contract availability gating.** `MissionRequirements` gains optional `minPrincipledScore` / `minMercenaryScore` / `patternGateLabel`. `meetsRequirements()` in MissionBoard runs the gate check against `getDecisionPattern()`. New chip on mission cards shows the human-readable label (e.g. "Underground-vetted client" or "Discreet client — no oversight") — **never a number**. New `requirementsWithOptionalPatternGate()` in the procedural generator: 60% of Difficulty-5+ contracts are open, 20% gate principled (Underground-vetted), 20% gate mercenary (no-oversight). Mismatched-pattern attempts disable ACCEPT with the hint "Client wants a different kind of operative — earn the track record."
- **M14p (Pass 2a) — NPC dialogue tone variants (CIPHER / NIGHTOWL_22 / Dispatch).** 8-entry dialogue catalogue in `libs/core/src/data/npcDialogue.ts`. CIPHER's first-advice message after 2 successful breaches; CIPHER's three-rules letter at 8 missions (deliberately does NOT fire for high-mercenary players — that silence IS the message); NIGHTOWL_22's first independent-contract pitch at 5 missions; VoidLink Dispatch's rank-3 promotion notice (different versions for principled vs mercenary — principled gets the "Stewardship contracts" unlock, mercenary gets the "Tier-2 Mercenary Listings" unlock); CIPHER's Underground induction (requires standing ≥ 50 + at least one whistleblower-protected choice, fires ONLY for principled patterns); three Arc-1-aftermath responses keyed off upload/destroy/sell. `evaluateDialogueTriggers()` fires on mission disconnect; each entry gated via `dialogue_fired_<id>` flag so it only fires once. Variants pick the right tone per pattern bucket via `pickDialogueVariant()`. **CIPHER literally stops greeting high-mercenary operatives.** 15 new unit tests (84 → 99).
- **M14p (pass 1) — Choice Architecture foundation + News Framing channel.** The keystone sprint that turns lore into mechanics. **No morality meter.** New `getDecisionPattern(player)` helper in `libs/core/src/engine/decisionPattern.ts` reads a catalogue of 18 choice flags (Arc 1 key choice, BLACK HALO turn/burn, civilian protection, whistleblower handling, operative-bounty acceptance, sell-vs-leak, target warning, identity-cleanup, false evidence, loan exploitation, contract refusals, Compact violations) and returns `{ principledScore, mercenaryScore, total, netScore, recentTrend, dominantTraits[], isReformer }` — **never rendered to the player as a number**. Back-compat shim maps the legacy `arc1_key_choice` string. New `frameNewsArticle()` token-substitution system in `libs/core/src/data/newsFraming.ts` adapts news text via 4 token buckets (`ACTOR_ADJ`, `ACT_NOUN`, `WORK_TONE`, `AFTERMATH`) across 5 pattern brackets. All 7 mission-type news templates refactored to use tokens; same sabotage mission now reads differently for mercenary vs principled players. 24 new unit tests (60 → 84 total). Pass 2 (3 remaining reflection channels + Reflection Scenes + 9-ending fan-out) next.
- **M14h.9 — World expansion: deep lore, Conviction architecture (planned), ongoing-world model, cosmetic catalogue, new creative docs.** A major creative/design pivot. Added §14a/14b/14c/14d to Full_Plan.md covering: the Year 2199 setting bible (the Collapse, Big Four, JCB, Underground, REVELATION), the Choice Architecture (no morality meter — accumulated decisions read by the world, surfaced via Reflection Mechanic), the Ongoing World cadence model (quarterly Darknet Drops + paid Chapters + anniversary events forever), and the full Cosmetic Catalogue (twelve always-available categories + Conviction Pass + earned-only items + anti-Destiny rules). Reframed Roadmap Phase 6 as "The Ongoing World". Added pre-launch sprint S2b/L11 for M14p Choice Architecture as a launch-blocker. Created three repo-root creative docs: `Why_Voidlink.md` updated, `Voidlink_Synopsis.md` (comprehensive game guide), `The_Voidlink_Codex.md` (Tolkien-depth lore bible with full timeline, faction profiles, named NPCs Mei Lin / Mira Kovac / Aino Virtanen / Yaakov Stern / Sonam Choedron / Eira Sandén / CIPHER / NIGHTOWL_22, the Voidlink Compact in full, glossary, calendar, music genres, famous operatives, philosophy schools). CLAUDE.md updated to register the new creative-doc category alongside the 5-doc planning rotation.
- **M14i — Research Tech Tree.** Meta-progression layer. New RESEARCH window (taskbar launcher) shows 5 branches × 5 nodes each (25 total): **CRYPTO** (Quantum Primer / Lattice Math / Zero-Day Library / Shor's Algorithm / Cryptographic Sovereignty), **STEALTH** (Quiet Boots / Phantom Routes / Forensic Static / Cold Footprint / Total Ghost), **HARDWARE** (Overclock Layer / Thermal Mastery / Cache Prefetch / Memory Bus Tuning / Custom Silicon), **SOCIAL** (Conversational Patterns / Pretexting / Insider Knowledge / OSINT Network / Social Engineering Mastery), **AI** (Curious Anomaly / Pattern Recognition / Synthetic Empathy / Cascade / Sovereignty — gated by `revelation_contact_count`). Each node has prereq + RP cost; Lattice Math (C2) discounts further Crypto nodes by 1 RP. Players earn **Research Points** on every mission completion: base 1 RP per difficulty tier, +1 for clean (no IDS), +1 for fully stomped wipes (plus A5 Sovereignty grants +1 RP per mission permanently). Effects wired now: C1+H1+C4 (crack speed), H3 (scan speed ×0.75), S2 (+1 relay hop, stacks with implant), H4 (+1 RAM slot), S3 (canary spike 25%→15%), S5 (baseRate −0.10 %/s). Story-gated AI branch lights up once `revelation_contact_count ≥ 1`. Social-branch nodes are placeholders for the phishing/OSINT modules in EA-S1/S2.
- **M14l — Physical-location gateways.** Where you're actually sitting matters. Four gateways: **Home** (default, neutral, free), **Safehouse** (15k Cr unlock + 5k Cr/in-game-week rent — trace baseline ×0.90), **Corporate VPN** (25k Cr — trace ×1.15 BUT notoriety accrual halved), **Tor Community Relay** (40k Cr — trace ×0.80). Rent ticks on the existing bank-interest loop; eviction back to Home if the player can't cover (with a `Property Management` inbox notice). New `unlockGateway` + `setActiveGateway` store actions. Effects applied at mission start (baseRate × gatewayBaseRateMul) and during notoriety accrual (× gatewayNotorietyMul). UI: new GATEWAY panel in OperativeProfile showing all four cards with acquire/switch/active states + remaining rent days.
- **M14k — Implants / Wetware.** Permanent neural/hardware augmentations purchased from an underground clinic. One-time, irreversible. Four implants seeded: **Ghost Reflexes** (Cr 80k, Underground +50 standing — log-wipes 20% faster, stacks with Ghost spec), **Brute Synapse** (Cr 120k, no faction gate — max relay-chain hops +1), **Architect Cortex** (Cr 200k, Voidlink Intl +100 — RAM concurrent-tool slot +1), **Quantum Inhibitor** (Cr 250k, Arunmor +50 — passive trace baseline −0.15 %/s). New `installImplant` store action + IMPLANTS tab in Upgrade Shop with grid layout, faction-standing chips, and INSTALL button. Each install dispatches a Darknet inbox email from "Underground Clinic" confirming the procedure. Effects wired through `relayHopBonus`, `ramBonus`, `wipeSpeedMultiplier`, and `traceBaseRateDelta` helpers in core.
- **M14j — Loadout slots.** Save/swap pre-mission setup (relay chain order + exfil channel + armed consumable) with one click. Three preset slots seeded on first interaction — **STEALTH** (DNS exfil, decoy log armed), **BRUTE** (direct FTP exfil, zero-day pack armed), **BANK RUN** (encrypted tunnel exfil, false flag armed). Custom slots can be created and renamed; presets cannot be deleted but can be overwritten. `LoadoutBar` component visible at the top of Mission Board and Bounce Chain Window; locks during active mission. Loadouts persist per save; `exfilChannel` hoisted from local NetworkMap state into `gameStore` (save v5).
- **M14f.1 — Canary files + timestomping.** IDS-protected data nodes (`file_server`, `database`, `mail_server`) now seed canary "honeypot" files at `tier × 10%` chance. Touching a canary auto-trips: +25% trace, +3%/s alarm for 15s, persistent `heat_<corp>` flag for the next mission. Detection requires `port_scanner_stealth`, `port_scanner_v3`, or `sniffer_v2` — otherwise canaries look like ordinary files. Two new tools added: `timestomper_v1` (Chrono Stomper, 4500 Cr, 30 REP) and `timestomper_v2` (Ghost Clock, 18 000 Cr, 180 REP). Without a stomper, even a clean wipe leaves a temporal fingerprint that raises cross-session heat on the corp; with one, every wiped node is also timestomped and clean-exit truly clears the flag.
- **M14h.8 — Docs consolidation (full 5-doc model).** Every planning, reference, and QA doc absorbed into exactly five files in `docs/`: `Full_Plan.md` (master plan + every system spec + multiplayer/modding/security/launch + AI disclosure + monetisation), `Complete_Tasks.md` (this ledger), `Next_Stage.md` (forward-looking only, world-class detail), `Roadmap.md` (visual timeline), `Testing_Guide.md` (per-milestone checklists + 19-phase end-to-end playtest). Removed: GAME_GUIDE, GAME_DESIGN_MASTER, PLAYTEST_WALKTHROUGH, all DEV_GUIDE_01-10, UPLINK_NG_OVERVIEW, DEV_DOCS_INDEX, ARCHIVE_*. README + CLAUDE.md updated to the 5-doc model.
- **M14h.7 — NetworkMap cyberpunk visual rework.** UnrealBloomPass (0.55 strength), ACESFilmic tone mapping, 600-point cyan starfield, scan-grid plane 8u below the graph, edges brightened to cyan so the topology reads as a live data-link diagram. Raycaster click logic untouched.
- **M14h.6 — Encrypted email inbox + mission relay-hop gating.** New `EmailInbox` window (sidebar + reader), `inbox` save-v4 store slice, mock-PGP fingerprints, encrypted-with-cipher-grid messages, mission-accept auto-dispatches contract email, INBOX taskbar launcher. Mission `minRelayHops` scaled D1=0 → D10=9 with "Build a N-hop relay on WORLD MAP" hint.
- **M14h.5 — Mid-game rebalance + plumbing.** Trace divisor 20→28, per-hop reduction 0.7→0.65, +PROXY/-PROXY HI buttons removed (route is the only bounceCount source), `getMaxRelayHops()` centralised (caps 3/6/8/10/12), sabotage briefing reworded for router OR admin_console, banking APRs inflated (Global 12% / Pacific 22% / Cayman 6% / Zurich 15%), `notorietyPerHour` per bank, `player.notoriety` accrues from balance × hours and adds +0.10 %/s to baseRate at mission start, login save-list staggered reveal + click SFX, global world clock VST anchored at real 2026-01-01 → game 2199-01-01.
- **M14h.4 — Signup email confirmation + password reset.** 6-digit DARKNET RELAY one-time code (visible in demo build), FORGOT link in operative-connect prompt, new `updatePassword` + `findSaveByEmail` persistence helpers.
- **M14h.3 — Neon-Earth Data Globes + UX polish.** UnrealBloomPass + real continent outlines from `world-atlas/countries-110m.json` + cyan/magenta palette on both GlyphDrift background and interactive WorldMap. Tutorial auto-focuses spotlit windows. `bounce → RELAY` rename across HI/Taskbar/SystemConsole. Node colour state: yellow when scanned, green when breached. 3-pulse intruder beep on rival hacker spawn. WorldMap bloom softened to 0.55 strength so target dots don't smear.
- **M14h.2 — Layout persistence + UX hotfixes.** Save v3 schema persists `activeWindows`, `windowLastPositions`, `windowZCounter`. HACK TOOLS no longer requires an active mission. WorldMap rotate-speed scales with zoom. Window `onMove` fires on resize too.
- **M14h.1 — Sabotage rebalance + dial-up SFX.** Sabotage trace rebalance (60s base + 15s/hop, lower spike). Audio master bus + volume responsiveness. HI/Bounce-Chain auto-open on desktop. Dedicated `BounceChainWindow`. Breach-acquired bounces added to library. Richer DTMF/handshake dial-up SFX.
- **M14h — Shop expansion.** 2 new HW slots (GPU, Cooling). 3 new SW categories (Sniffer, MemScrape, Anti-Forensic). 5 tier extensions (Cracker v5, Proxy v4, etc.). 7 consumables with armed-flag effects (panic kit, zero-day pack, decoy log, false flag, rep tokens, cred pack) wired into crack speed / scan / heat / breach reveal.
- **M14g — Skill-tree Shop.** Upgrade Shop redesigned as SVG node-link graph (10 columns, prereq edges, colour-coded states, side detail panel, LIST fallback).
- **M14o — Choice missions.** `MissionChoice` + `pendingChoiceFromPhaseIndex` state, full-screen `MissionChoiceOverlay`, BLACK HALO mission with TURN-vs-BURN fork (different faction consequences + skipped phase).
- **M14n — Mission runtime events.** 2–5 procedural events per contract trigger on trace/time/breach, surface as toast banners (good/bad/neutral colours), real trace-rate modulation effects.
- **M14m — Multi-phase missions.** `MissionPhase` state machine, phase progress UI in HI, PROJECT GHOST 3-phase example (OSINT → Breach → Decoy), per-phase rewards, news echoes after disconnect.
- **M14f — Exfiltration channels.** 4 channels (Direct FTP / Encrypted Tunnel / DNS Tunneling / ICMP Exfil) with speed-vs-stealth tradeoffs + tool/spec gating, selector bar in NetworkMap. Partial (canary files + timestomping deferred to M14f.1).
- **M14e — Banking polish.** Sabotage → -15% stock drop on listed corps. MARKET CRASH world event (stocks crash, savings APR zeroed). Loan defaulting (-50 REP + news article when principal > 5× liquid).
- **M14d — UX & balance.** Mandatory log wipe + WIPE ALL button. OPEN WORLD MAP from HI. Connection-effect overlay (dial-tone + animated chain). Clickable Corp/Gov/Underground targets with TARGET INTEL window. Sabotage router injection. Rep gating rebalanced.
- **M14c — Banking expansion.** Loans. Cr↔Darkcoin trading. Equities (ARMR / ARES / INTC / GTBK). Offshore banks (Cayman + Zurich). Tabbed bank UI. Market simulation.
- **M14b — Banking foundations.** Bank window, deposits/withdrawals, savings interest.
- **M14a — Pre-alpha polish.** Settings menu (⚙) with music/SFX/UI-scale/reduce-motion/FPS toggles. Idle music with mission-active fade. Trace beep proximity system. Window position memory. In-game clock (epoch 2199-01-01). Bounce network moved to WORLD MAP globe. Neon digital globe with country outlines. Game loop perf throttle (~75% CPU drop). Mission retry on incomplete disconnect. Cracker version mapping fix. Light theme with full CSS variable system. Tutorial overhaul to 25-step spotlight (no hard blockers). Audio polish (master buses, click/tick/window-open/close/error SFX, autoplay-policy-safe resume). Accessibility (`<main>` landmarks, `@fontsource` self-hosted fonts).
- **M15 — Privilege escalation + persistent backdoors.** ESCALATE on breached nodes (requires CPU≥3 + Cracker v3+, +tier×2.5% trace). PLANT BACKDOOR after root, pre-breaches the node on future missions against the same corp via `backdoor_<corp>_<type>` flag. Traffic-sniffer partial via M14h Sniffer tools.

---

## 2026-05-28

- **M13 — Service-specific exploits + brute lockout + subnet zones.** Protocol-aware crack durations, per-service vulnerabilities. Brute-force lockout: after N failed cracks a node locks until network reset. Subnet zones Zone A / Zone B with pivot-node gating; pivots show orange when unscanned.
- **M12 — Lateral movement + credential reuse + memory scraping.** Dump credentials on breach; reuse them to skip cracks on connected nodes sharing creds. `memscrape` tool pulls in-memory creds.
- **M11 — Bounce log wipe sub-missions + hop health.** Per-hop log-status (clean / dirty / traced). Dedicated wipe sub-missions to clean dirty hops in the bounce library.

---

## 2026-05-27 — Initial pre-alpha sprint

- **XP & Level System.** Rank 1–1000 curve, XP per mission, rank thresholds, level-up notifications.
- **M01–M10 — Core hacking loop.** Boot → login → tutorial → first crack → mission → trace → upgrade loop. 9 procedural mission types. Multi-rate trace system. 3D NetworkMap (Three.js). Bounce network foundations. 3D WorldMap globe. System Console overlay. Operative Profile tabs. 4 specialisations. 7 world events. Rival hacker AI. Electron wrapper. i18n scaffold.
- **Story Arcs 1–5 (initial cut).** REVELATION (Arunmor) → UNDERGROUND (Cipher) → ARES initial encounter → NIGHTOWL freelance → VOIDLINK INTERNATIONAL meta-arc. Five endings authored (CONTAINMENT / LIBERATION / SOVEREIGNTY / ERASURE / GHOST).
- **i18n + accessibility scaffold.** `i18next`. axe-core integration. Keyboard navigation. `prefers-reduced-motion` respect.
- **Audio engine.** SFX engine. Dial-up sequence (DTMF + handshake). Scan / crack / wipe / breach / error / success effects. Trace beep with proximity scaling.

---

## 2026-04 → 2026-05 — Foundations

- **Monorepo bootstrap.** pnpm workspaces + Vite + React 18 + TS strict. `@voidlink/core` engine package extracted, no UI dependencies.
- **Three.js scaffolding.** NetworkMap + WorldMap initial cuts.
- **Zustand + Immer store.** All game state in one store, immutable updates via Immer.
- **Save/load.** Per-handle localStorage persistence with auto-save every 60s.
- **Design docs.** Initial master design + per-system dev guides (since consolidated into `Full_Plan.md`).
